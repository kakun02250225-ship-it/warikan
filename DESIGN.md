# 設計書：貸し借り ＋ 家計簿アプリ

## 0. 前提（方針・要変更なら連絡）

| 項目 | 決定 | 理由 |
|---|---|---|
| プラットフォーム | React Native (Expo) | 日常スマホ利用 / iOS・Android両対応 / TS |
| クラウド | Firebase (Auth + Firestore) | 双方向リアルタイム同期・認証が標準装備 |
| 状態管理 | Zustand（軽量）| Reduxは過剰 |
| 言語 | TypeScript | – |

> データモデルはプラットフォーム非依存。スタックを変えても §4 はそのまま使える。

---

## 1. 目的・スコープ

**目的**：1つのアプリで「個人の家計簿」と「人ごとの貸し借り」を管理する。貸し借りは相手ともクラウドで共有し、片方が記録すると相手側にも反映される。

**やること（MVP）**
- 家計簿：支出/収入の記録、カテゴリ別集計、カレンダー、円グラフ
- 貸し借り：人ごとの台帳、借/貸の記録、相手との双方向同期

**やらないこと（MVP除外）**
- 家計簿と貸し借りのデータ連携（貸した金の自動支出計上など）
- 3人以上のグループ割り勘
- 広告・課金・多通貨

---

## 2. 機能要件

### 2.1 家計簿（個人・非共有）

**事実（画像7〜9）**
- タブ：支出 / 収入 /（貯金額）
- 入力：金額・カテゴリー・メモ・日付・保存
- カテゴリ例：食費・外食費・日用品・交通費・衣服・交際費・趣味・その他
- カレンダー画面：日別の支出額、月の収入/支出/合計サマリ、日付別明細
- グラフ画面：月選択、カテゴリ別円グラフ、カテゴリ別合計リスト

**方針**
- 家計簿は「自分だけのデータ」。クラウドはバックアップ用途のみ、共有しない。
- 貯金額タブは MVP では「収入合計 − 支出合計」の表示に留める（積立目標機能は後回し）。

### 2.2 貸し借り（人ごと・共有可能）

**事実（画像1〜6）**
- 相手（人）ごとに台帳を持つ。人は追加・切替可能（例：ゆうま / やまとまと）
- 入力：金額・メモ・日付（カレンダー選択）・借/貸の選択（借=オレンジ, 貸=緑）
- 一覧：取引カード（日付・金額・メモ）、借/貸フィルタ
- 収支表示：`貸の合計 − 借の合計`
  - 検算：36,900（貸）− 24,210（借）= **+12,690**（画像2/5と一致・確認済み）
- 人の登録：名前入力 → 登録、人ごとに収支を表示

**方針（核心）**
- 相手は2種類：
  1. **ローカル相手**：ただのラベル。アプリ未使用の人でも記録できる（共有なし）
  2. **接続相手**：アプリ利用者と紐付け。招待で接続し、**双方向同期**する
- 「佑真側でも反映」= 台帳を両者で共有し、**1レコードを共有**する。
  自分視点で「貸」なら、相手視点では「借」として符号反転で表示（コピーは作らない）。

---

## 3. 技術スタック

| レイヤ | 採用 |
|---|---|
| アプリ | React Native + Expo (TypeScript) |
| 画面遷移 | Expo Router |
| 状態 | Zustand |
| 認証 | Firebase Authentication（匿名 → 後でメール昇格）|
| DB | Cloud Firestore（オフラインキャッシュ有効）|
| グラフ | react-native-svg / victory-native |
| 日付 | dayjs |

**代替案**：Flutter + Supabase（Postgres＋RLSで権限を厳密にしたい場合）。今回はセットアップの速さを優先し Firebase を推奨。

---

## 4. データモデル（Firestore）

```
users/{uid}
  name: string
  email?: string

# 家計簿（個人・非共有）
users/{uid}/records/{recordId}
  type: "expense" | "income"
  amount: number          # 正の整数（円）
  category: string        # 食費 / 交通費 ...
  memo?: string
  date: timestamp

# 貸し借り台帳（人ごと・共有可能）
ledgers/{ledgerId}
  ownerUid: string        # 作成者
  memberUids: string[]    # [ownerUid] or [ownerUid, partnerUid]
  partnerLabel: string    # 表示名（例: "ゆうま"）
  partnerUid?: string     # 接続相手のuid（未接続ならnull）
  createdAt: timestamp

ledgers/{ledgerId}/entries/{entryId}
  creatorUid: string
  amount: number          # 正の整数（円）
  direction: "lend" | "borrow"   # ★creatorUid視点
  memo?: string
  date: timestamp
```

**符号の解釈（重要）**
- `direction` は必ず `creatorUid` 視点で保存する。
- 相手が見るときは `viewerUid !== creatorUid` なら `lend↔borrow` を反転して表示。
- 台帳の収支（ある視点 `me` から）：
  `Σ(自分視点でlend) − Σ(自分視点でborrow)`
  → プラスなら「相手にそれだけ貸している」。

---

## 5. 同期・共有の仕組み（双方向反映）

**接続フロー**
1. Aが相手ラベル「佑真」の台帳を作成（この時点はローカル相手）
2. Aが招待リンク/コードを発行 → 佑真に共有
3. 佑真がコードで参加 → `ledgers/{id}.memberUids` に佑真uidを追加、`partnerUid` を相互設定
4. 以降、どちらが `entries` に追記しても、両者のアプリにリアルタイム反映（Firestoreリスナー）

**反映の実体**
- レコードは1件のみ（コピーしない）。表示側で符号を反転するだけ。
- → 二重計上・更新競合が原理的に起きない。

**セキュリティルール（要点）**
- `users/{uid}/records/**`：本人のみ read/write
- `ledgers/{id}` と配下 `entries`：`request.auth.uid in memberUids` のみ read/write
- `entries` 作成時 `creatorUid == request.auth.uid` を強制

---

## 6. 画面一覧

| # | 画面 | 内容 | 元画像 |
|---|---|---|---|
| 1 | ホーム（タブ切替）| 家計簿 / 貸し借り の2大機能へ | – |
| 2 | 家計簿・入力 | 金額・カテゴリ・メモ・日付・保存 | 9 |
| 3 | 家計簿・カレンダー | 月表示、日別支出、月サマリ、明細 | 8 |
| 4 | 家計簿・グラフ | カテゴリ円グラフ、カテゴリ別合計 | 7 |
| 5 | 貸し借り・人一覧 | 人ごとの収支、人の追加/切替 | 5,6 |
| 6 | 貸し借り・台帳詳細 | 取引カード、借/貸フィルタ、収支 | 2,3,4 |
| 7 | 貸し借り・入力 | 金額・メモ・日付・借/貸 | 1 |
| 8 | 台帳の共有 | 招待コード発行 / 参加 | （新規）|

---

## 7. 認証

- 起動時に **匿名認証** で即利用開始（家計簿だけならログイン不要感）。
- 貸し借りを共有する段階で **メール/パスワード（または Google）へ昇格**。
- 匿名→本認証の昇格は `linkWithCredential` で uid を維持（データを失わない）。

---

## 8. 開発フェーズ

**Phase 1（MVP・単独動作）**
- 家計簿：入力 / カレンダー / グラフ
- 貸し借り：人の追加、ローカル相手での借/貸記録、台帳収支
- データは Firestore（自分のuid配下）に保存

**Phase 2（共有）**
- 認証昇格、招待コードによる台帳接続、双方向リアルタイム反映
- セキュリティルール適用

**Phase 3（拡張・任意）**
- 3人以上のグループ台帳、家計簿×貸し借りの連携、CSV出力

---

## 9. Claude Code への指示メモ

- Phase 1 を最優先。**動くものを最小構成で**。先回りの抽象化・未使用の共通化はしない。
- 金額は整数（円）で保持。浮動小数を使わない。
- `direction` は常に creator 視点。表示反転ロジックは1箇所（util）に集約。
- Firestore は最初からオフライン永続化を有効化。
- 画像はイメージ。ピクセル完全一致は不要、情報構造を優先。
